import random
import string
length=int(input('enter length of password:'+' '))
character=string.ascii_lowercase+string.ascii_uppercase+string.digits+string.punctuation
print("password is",''.join(random.sample(character,length)))
